# 文档模式（写作模式）能和真实 rust typst 一样渲染吗

> 调研时间：2026-09-15。**仅新增本文档，无任何代码改动。**
>
> 方法：一手资料优先——本地 vendored 的 typst 0.15.1 各 crate 源码
> （`~/.cargo/registry/src/*/typst*-0.15.1/`，即本仓库实际链接的版本）、GitHub `typst/typst`
> v0.15.1 tag 的源码、官方文档/changelog，逐条给出行号或链接。
>
> **本文的 API 事实是核对过的；第三节之后是设计论证，阶段 0 探针尚未实跑**（本次会话沙箱只读）。
> 未验证项集中列在第九节，别把它们当成结论。

---

## 结论（先看这段）

1. **写作模式现在的渲染是"用 CSS 复刻 typst 的观感"，不是 typst 的排版结果。** 正文断行、
   字距、断词、`#set` 规则、前缀里的宏，全都不来自引擎。调参只能更接近，性质不会变。
2. **"和真实 rust typst 一样渲染"只有一条路：显示引擎自己的产物** —— 整篇编译一次，把每个
   源块在页面上的那一块切下来当渲染结果，光标所在的块展开成源码。
   **本仓库的公式 widget（`compile_math` → 内联 SVG）就是这条路的最小版本**，把它从"公式"
   推广到"块"就是全部工作。
3. **这条路已经被同栈的独立实现走过**（Tauri v2 + CodeMirror 6 + 内嵌 typst 0.15，无 wasm，
   见第六节），它把形态命名为「渲染表面 + 源码透镜」，并留下了"两个各自排版的系统无法共存"
   的实证。代价必须认下来：**正在编辑的块显示源码；渲染块不可跨块选择/复制；页级特性
   （页眉页脚 / 页码 / 分页 / 多栏）降级为源码。**
4. **HTML 导出不是答案**：0.15.1 仍是 feature flag，官方文档写"不要用于生产"；它的输出
   **不带任何源位置**，且**没有分页**（第七节）。
5. **真要动手，先做阶段 0 探针（`geometry_for_range`），不要先动前端。** 唯一的未知是
   "源块 ↔ 版面区域"的切分质量与性能，先量它（第八节）。

---

## 一、现状差在哪（逐条对照，带证据）

| 维度 | 写作模式（现在） | 真实 typst | 证据 |
| --- | --- | --- | --- |
| 正文字号/行距 | 16px、`line-height: 1.9`、左右各 48px | `text` 默认 11pt、`par.leading` 默认 0.65em、`par.spacing` 1.2em、`par.justify` 默认 **false** | `src/lib/Editor.svelte:436,479-484`；`par.rs` 的 `#[default(Em::new(0.65).into())]` 与 `#[default(false)]` |
| 字体 | 编辑器 CSS 字体栈（`Noto Serif CJK SC` → Georgia 兜底） | 一律来自 typst 的 FontBook（Libertinus Serif + 打包思源宋体） | `Editor.svelte:444-446`；打包字体只喂 typst（`typst_world.rs:378`），前端**没有** `@font-face` |
| 标题 | 1.8 / 1.5 / 1.25 / 1.08em | 1.4 / 1.2 / 1.0em，above 1.8em、below 0.75em | `Editor.svelte:492-515`；`heading.rs` 的 size ladder + `above`/`below` |
| 断行/断词/字距 | 浏览器折行（`break-spaces`） | 引擎自己的断行 + 断词（`hyphenate`）+ CJK 规则 | 同一段文字断点必然不同 |
| `#set` / `#show` / `#let` / `@preview` 包 | CSS 里不存在，写进文档就是一行可见源码 | 真执行 | **设置里的前缀代码尤其明显**：前缀写 `#set text(size: 12pt)` 会改 PDF，写作模式毫无反应（`+page.svelte:1442` 只把它并进编译源） |
| 列表 | `- ` 文字替换成「• 」、`+ ` 按缩进计数 | 真列表（marker 对齐、缩进、间距、编号格式） | `src/lib/markup-ranges.ts:129,133` |
| 代码块 | 自绘 div + `rgba(128,128,128,0.14)` 灰底 + 圆角 | `raw` 默认只有等宽字体（要底色得自己 `#show raw: block(fill: …)`） | `src/lib/live-preview.ts:532-545`；`typst-library/src/text/raw.rs:407` 的文档示例 |
| 公式 | ✅ **唯一真走引擎**（`compile_math` + 贴边 SVG + 基线对齐 + 墨迹撑画布） | 同样 | `src-tauri/src/typst_world.rs:720`，见 `docs/WYSIWYG-调研.md` |
| 表格/图/引用/脚注/交叉引用/编号/分页 | 显示源码 | 真排版 | `docs/WYSIWYG-调研.md:201` 自认 |
| 暗色 | 对 typst 产物整体 `filter: invert(1)` | 文档自带颜色会被反掉 | `live-preview.ts:516` |

一句话：**能"看起来像"，不能"是"。** 差距不在参数，在"结果不由引擎产生"。

## 二、为什么 CSS 这条路永远到不了

- 文字排版是引擎算法的产物（行断点、伸缩、断词、CJK 禁则、`par.justify`），浏览器不会算出
  同一个结果。
- `#set text(size:)` / `#show` 规则 / `#let` 函数 / `@preview` 包是**代码**，CSS 无从执行。
  写作模式面对它们只有两个选择：无视（现状），或者把它当普通文本显示。
- 因此"一样"只能等于**显示引擎的产物**。这也是为什么公式那条路是现在唯一"真"的部分。

## 三、可行方案：渲染表面 + 源码透镜（块级切片）

### 3.1 原理

1. 整篇编译一次（本仓库**已经**每次按键全量编译：`+page.svelte:1318` 无防抖 → `:1435 runCompile`），
   页高用 `page(height: auto)`（`compile_math` 已在用），版心宽取编辑器正文列宽（复用 0.7.9 的
   `compile_with_page_width`，`typst_world.rs:599`）。
2. 在编译产物（页帧）里，把每个**源块**对应的渲染区域算出来（页号 + x/y + 宽高 + 基线）。
3. 编辑器里：**非光标块**用 `Decoration.replace` 换成该区域的 SVG 切片（块级 widget）；
   **光标/选区进入的块**不挂 widget，源码自然露出 —— 与现有公式 widget 完全同一套机制。
4. 点击切片 → 用帧里的字形位置反查源码偏移，把光标落到**正确字符**（不是块首）。

### 3.2 需要的 API（逐个核对过 0.15.1 源码）

| 用途 | API | 位置 |
| --- | --- | --- |
| 逐字形源码映射 | `Glyph.span: (Span, u16)`（span + 节点内子偏移） | `typst-library-0.15.1/src/text/item.rs:109` |
| 字形位置 | 自己累加 `glyph.x_advance.at(text.size)`（没有现成绝对坐标数组） | 官方同样做法见 typst-ide `jump.rs` 与 `typst-svg/src/text.rs` |
| 元素级源位置 | `FrameItem::Shape(_, Span)` / `Image(_, _, Span)` 带裸 `Span`；`FrameItem::Tag(Tag)` → `Tag::Start(Content)` → `Content::span()` | `typst-library/src/layout/frame.rs:486`、`introspection/tag.rs:12` |
| 块在页面上的位置 | 页帧里按序压入的 `Tag`（带位置）：`tags.extend(...)` → `frame.push_multiple(pos, FrameItem::Tag(…))` | `typst-layout-0.15.1/src/pages/mod.rs:237` |
| 元素 → 点 | `PagedIntrospector::position(location) -> Option<PagedPosition>`（**只有一个点，没有宽高**） | `typst-layout/src/introspect.rs:57`；`PagedPosition{page, point}` 见 `introspection/position.rs:61` |
| 点 → 源码（字符级） | `typst_ide::jump_from_click` / `jump_from_click_in_frame`（会反转 group 变换、按 clip 剪裁、按点击落在字形左右半再加半字） | GitHub v0.15.1 `crates/typst-ide/src/jump.rs` |
| 源码 → 点 | `typst_ide::jump_from_cursor(document, source, cursor) -> Vec<PagedPosition>`（**每页最多一个点**，内部 `find_in_frame` 是私有的） | 同上 |
| 任意 Frame → SVG | `typst_svg::svg_in_html(frame, text_size, pretty, id, styles, anchors, link_resolver)` | `typst-svg-0.15.1/src/lib.rs:82` |
| 切一块 Frame | `Frame::items()/size()` + 公开 mutator `resize` / `translate` / `transform` / `clip(Curve)` / `retain(f)` | `typst-library/src/layout/frame.rs:147,280,293,345,352,230` |
| 整页 → SVG（现状） | `typst_svg::svg(page, &SvgOptions)`，`Page` 字段全 pub，可自造页 | `typst-svg/src/lib.rs:32`；`typst-layout/src/document.rs:83` |
| 输出里的源信息 | **没有任何格式带源位置**：SVG 只写 `data-typst-label`（仅带 label 的 group），`FrameItem::Tag` 完全不进 SVG；HTML 只写 `data-lang` | `typst-svg/src/lib.rs:348,321`；`typst-html/src/rules.rs:740` |

**关键推论**：源位置的唯一来源是**内存里编译出来的帧/DOM**，不是任何导出格式 ⇒ 想做这件事
只能用内嵌引擎（本仓库的形态），WASM 方案（typst.ts）在这方面帮不上忙，而且它用的是另一套
渲染后端（`typst-preview` 的 README 自己声明"不保证渲染结果一致"）。

### 3.3 前端可复用的既有机制

| 已有 | 位置 | 复用方式 |
| --- | --- | --- |
| `Decoration.replace` + 块级 widget + pt 尺寸 + 基线对齐 | `src/lib/live-preview.ts`（`MathWidget` / `MathBlockWidget`） | 换内容，机制不变 |
| 点击 widget → 光标回源码 | `live-preview.ts:135-139` | 升级为"点哪个字落在哪个字" |
| 渲染请求去重 / 防抖 / 缓存 / 失败退回源码 | `+page.svelte` 的 `mathCache` + `compileMath` 批处理 | 缓存键改为「块源码 + 上下文 + 版心宽 + 字号」 |
| 版心宽 → 页宽 pt 的换算 | `src/lib/preview-scale.ts` + `typst_world.rs:572` | 直接沿用 |
| 真实产物夹具 + 浏览器几何断言 | `scripts/browser-check/wysiwyg-visual.mjs`、`npm run fixtures:math` | 阶段 1 的验收手段 |

### 3.4 已知代价（必须提前认）

- **正在编辑的那一块必须是源码**。这是 Typora 模型的固有性质（连 Typora 的活动行也是 markdown），
  不是实现缺陷。
- **渲染块是图片**：跨块拖选、复制、查找、拼写检查、无障碍都拿不到。补救是二期加"文字层"
  （按字形位置铺绝对定位的 span，pdf.js 那套思路），能做但属于额外工作。
- **单栏里没有"页"**：`#set page`、页眉页脚、页码、分页、脚注落页底、多栏、`#place` 这些
  按定义与 PDF 不同 → 这些块降级显示源码。
- **暗色**：要么保持白纸（推荐），要么反色（文档自带颜色会失真）。
- **性能**：编译次数不变，新增 N 次小区域 SVG + 每块几何计算；需按视口取块并缓存。

## 四、机制区分：整篇切片 ≠ 片段重编（别被吓退）

外部资料列的"块级重渲染会坏"（计数器、`@ref`、`#outline`、脚注、`context`、`#place`）说的是
**把片段单独拿出来重新编译**的坏法——那种做法下编号、引用、脚注内容全是错的。

**我们的方案是从整篇编译的产物上切片**，所以数字、引用、脚注内容全对；真正的问题只是**几何**：

- 脚注正文落在页底 ⇒ 它的区域与引用它的段落不连续；
- `#place` / `page.background` 的内容不属于任何流式块；
- 内容可能超出"页"的边界（typst 允许画到帧外，0.7.6 的公式下标被裁就是这一类）。

⇒ 因此切分规则应当是**"按流式块在页面上按 y 序切带"**，而不是"按 span 求并集"。
`FrameItem::Tag`（按位置压入页帧，见 3.2 表）给的正是"流式块从哪个 y 开始"，这是切带的天然依据。
这条是阶段 0 要量的核心。

## 五、实现要点与坑（本节是推断 + 源码依据，未实测）

1. **增量编译要先接上**：本仓库现在**每次编译都新建 World + 新建 `Source`**
   （`typst_world.rs:135,169`，四处 `TypstWorld::new`），comemo 只能命中不依赖 Source 的层。
   官方姿势（`typst-cli watch`）：World 建一次 → 每次只 `world.reset()`（清文件/时间缓存，字体不清）
   → 用 `Source::replace()`（`typst-syntax/src/source.rs:85`，内部 `edit` 做增量重解析）原地更新
   → 编译后 `comemo::evict(10)`。要做"每按键重切块"，这条基本是前提。
2. **`today()` 是 comemo 的失效源**：`TypstWorld::today()`（`typst_world.rs:276`）每次调用重新取
   `SystemTime::now()`，文档一碰 `datetime` 就会全 miss。
3. **comemo 缓存是进程全局的**，而本应用支持 `Ctrl+Shift+N` 多窗口 ⇒ 多窗口必须各用不同
   `FileId`，否则互相命中错结果（未保存文档目前统一 `main.typ`，这条要查）。
4. **坐标系与变换**：`FrameItem::Group` 要反转 `transform`、按 `clip` 剪裁才能映射回本层
   （`jump_from_click_in_frame` 就是这么做的；tinymist 自己抄的那份留着 `// TODO: Handle
   transformation.`，说明这是真坑）。现有 `ink_bounds_of_frame`（`typst_world.rs:874`）**只算纵向、
   不看 span、不处理 clip**，必须扩成二维 + 认 span + 处理 clip。
5. **span 与 Source 必须配对**：`Span` 是编译期那份 `Source` 的编号，文本一改，改动点之后的
   编号会漂 ⇒ **click→source 必须用"编译时的那份 Source"**，不能拿最新文本去解旧帧。
6. **typst-ide 要锁版本**：0.15.1 在 crates.io（Apache-2.0）可直接依赖，但它是 pre-1.0 且与
   typst 同步发版；最成熟的消费者 tinymist **没有用它**，自己抄了一份等价实现。⇒ 用它可以，
   但升级 typst 时要预期签名变化。另：0.15.1 的公开面只有
   `jump_from_click / jump_from_click_in_frame / jump_from_cursor / tooltip / definition /
   autocomplete / analyze_* / matchers`，**没有 `highlight` / `references` / `jump_from_position`**
   （0.12–0.15 都没有）——别按旧教程设计。
7. **别指望给 SVG 打标记**：0.15 还**移除了** SVG 元素上的 `typst-frame` / `typst-doc` /
   `typst-group` / `typst-shape` / `typst-text` 类（0.15 changelog），"按 class 找帧"这条路双重死掉。
8. **切片渲染两种实现各有取舍**：① 每块单独 `svg_in_html`（产物小、IPC 小，但要 N 次渲染）；
   ② 整页渲一次 + 前端按 `viewBox` 包一层（零额外渲染，但每块要复制一份页面 SVG，DOM 体积大；
   同文档 `<use href="#…">` 也可能可行但未验证）。阶段 0 顺带量这个。

## 六、生态与先行者（含许可证）

| 项目 | 形态 | 结论 |
| --- | --- | --- |
| **[flatflax/typst-editor](https://github.com/flatflax/typst-editor)** | **Tauri v2 + TS + CodeMirror 6 + 内嵌 typst 0.15（无 wasm），与本仓库同一套栈** | **最直接的前例**。Rust 侧 `geometry.rs` 的 `geometry_for_range` / `block_geometry`："walk the compiled `Frame` tree for a source byte range and return rendered geometry (page, x/y, width/height, baseline)"——**把 `jump_from_click`/`jump_from_cursor` 的 span 匹配从一个点推广到一个区间**，正是阶段 0 要写的东西。其 M15 结论："**光标/选区不能跨两个各自排版的系统（PM/CSS vs typst）渲染同一区域**"，据此放弃"近似渲染 + 第二套排版引擎"，把形态定为「渲染表面 + 源码透镜」，并明确"真 WYSIWYG"只是愿景。⚠️ **许可证 GPL-3.0 → 本仓库是 MIT，只能读思路，不能抄代码** |
| [tinymist](https://github.com/Myriad-Dreamin/tinymist) | LSP + 预览（3532★，活跃，Apache-2.0） | 映射同样做在编译出的 `Frame` 上（`crates/tinymist-query/src/jump.rs` **自己实现了 jump**，未依赖 typst-ide） |
| [typst-preview](https://github.com/Enter-tainer/typst-preview) | 已**归档**（"use tinymist instead"，MIT） | 用的是 typst.ts 的**另一套渲染后端**，README 自认"不保证渲染结果一致"——反证"保真"必须用官方 Rust 引擎 |
| [typst.ts](https://github.com/Myriad-Dreamin/typst.ts) | WASM（Apache-2.0） | 有实验性增量 SVG diff（`renderSvgDiff`）与 `getSourceLoc(path)`，但产物不带源信息、后端不同 ⇒ **对本仓库无价值** |
| 官方 typst web app | 源码 + 预览（**闭源**） | click-to-jump 自 2023 就有；路线图里没有任何 WYSIWYG 条目 |
| ortic/typst-wysiwyg（MIT）、texpile（AGPL-3.0）、TyX | 第二套排版引擎 / 自有文档模型 | 都不是"保真"路线 |

## 七、HTML 导出为什么不是答案

- **0.15.1 仍在 feature flag 后面**：官方文档原话"still **very incomplete** … **Do not use this
  feature for production use cases**"，[跟踪 issue #5512](https://github.com/typst/typst/issues/5512)
  明确"没有脱离 flag 的时间表"；代码侧也在：`typst-library/src/lib.rs` 里
  `if features.is_enabled(Feature::Html)`，否则 `compile` 直接报错。
- **输出里没有源位置**：整份 `typst-html` 只写一个 `data-lang`（`src/rules.rs:740`）。span 只在
  内存的 `HtmlNode::Text(text, span)` / `HtmlElement.span` 上（`src/dom.rs:105,197`）。
- **没有分页**，且图形类元素（rect/line/polygon/circle…）在官方跟踪表里是 ❌。
- 它适合的是"语义化 HTML"，官方文档也把目标写成"human-readable, editable by hand and downstream
  tools"，**不是**给编辑器做保真渲染用的。

## 八、推进计划与验收

| 阶段 | 做什么 | 量级 | 验收 |
| --- | --- | --- | --- |
| **0（已完成，见第九节）** | Rust 侧探针 `geometry_for_range`（点→区间的推广）+ `#[test]` 打印：块覆盖率 / 是否按 y 序连续 / 单块与整页渲染耗时 | 半天，**不改产品行为** | ✅ 5 份真实文档已跑：24/25 块拿到几何、4/5 篇 y 序单调 |
| 1 | 新增 `compile_blocks` 命令 + 前端**只读**块 widget（不改交互） | 2~4 天 | 复用 `scripts/browser-check/wysiwyg-visual.mjs` 的路子：把真实产物做成夹具，断言"各块切片按 y 序拼接 = 整页渲染"（几何比对） |
| 2 | 点击→精确字符、进出展开、缓存/防抖、滚动锚定、暗色策略、诊断波浪线对位 | 2~3 天 | `scripts/browser-check/wysiwyg.mjs` 加一组交互验收（真实按键 + 真实选区） |
| 3（可选） | 文字层（选择/复制/链接可点）、列表项级细粒度 | — | — |

**不建议**走 Texpile / Tylina 那种"自有文档模型 + 双向映射"（`docs/WYSIWYG-调研.md` 已论证：
typst 图灵完备 ⇒ 双向映射不可靠，渲不出来的必须降级成代码碎片）。那是重构级投入，换来的是
可编辑性，不是保真度。

## 九、阶段 0 实测结果（2026-09-15 已跑）

探针代码：`src-tauri/src/block_geometry.rs`（`#[allow(dead_code)]`，只被测试调用，**不改产品行为**）。
运行：`cargo test --manifest-path src-tauri/Cargo.toml dump_block_geometry -- --ignored --nocapture`
（另有 `geometry_finds_typical_blocks` 常驻单测；`cargo test` 现为 37 passed / 2 ignored。）

版心宽 495pt ≈ A4 去掉页边距；编译源注入 `#set page(width: 495pt, height: auto, margin: …)`
（**单张长页，无分页**）。debug 构建。

| 样例文档 | 块（有几何/总） | 正文覆盖率（字符） | 行序单调 | 相邻块空隙 | 编译 | 整页渲一次 | 逐块切片合计 | 切片产物 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 散文（中文 + 行内公式） | 4/4 | 89.0%（89/100） | ✓ | +0.43 … +11.35pt | 2304ms※ | 11.9ms | 12.8ms | 84KB |
| 列表 + 有序列表 + 行间公式 | 9/9 | 73.1%（49/67） | ✓ | +1.54 … +11.98pt | 26.3ms | 6.8ms | 11.5ms | 70KB |
| 代码块 + 表格 + 强调 | 5/5 | 82.2%（83/101） | ✓ | +5.02 … +11.45pt | 474ms | 6.5ms | 5.7ms | 48KB |
| 宏 / `#show` / 脚注 | 4/5 | 43.6%（58/133） | ✗ | **−54.72** … +5.62pt | 28.3ms | 11.0ms | 15.1ms | 96KB |
| 长段落 `#lorem(180)` | 2/2 | 86.7%（13/15） | ✓ | +4.19pt | 48.7ms | 25.8ms | 7.7ms | 38KB |

※ 第一篇含冷启动（FontBook / Library 首次构建）；后面几篇是热态。均为 debug 构建，Release 未测。

**四条结论（都是实测，不是推断）**：

1. **切得出来。** 25 个块里 24 个拿到几何；唯一"没有几何"的正是那两行 `#let` / `#show`
   ——它本来就不渲染。也就是说"没几何的块"恰好等于"该显示源码的块"，判据天然对齐。
2. **能像积木一样按 y 序堆叠。** 4/5 篇 y 单调、相邻块空隙全是正数（+0.43…+11.98pt）：
   块之间不重叠，切出来直接按顺序摞起来就还原整页。
3. **唯一坏掉的是脚注，而且正是调研文档第四节预言的那一类**：`#footnote[...]` 所在段落的
   bbox 被拉到页底（y 76.6→158.9，高 82.3pt），与紧随其后的标题重叠 −54.72pt，该篇正文覆盖率
   掉到 43.6%。⇒ **必须按"流式块在页面上的 y 序切带"，不能按 span 求并集**，这条从推断升级为实测。
4. **切片渲染成本可控。** 第一版把整页项全压进切片时，8 块的切片合计 **100.8ms**（整页才 17.6ms，
   反而贵 6 倍）；改成"只压入与切片矩形相交的项"后降到 5.7~15.1ms，与整页渲一次同量级。
   产物每块 5~20KB（整篇 38~96KB）⇒ **"一块一渲 + 走 IPC"可行**，不必走"整页渲一次 + viewBox 复制"。

**两个只有跑过才知道的坑（已写进代码注释）**：

- **`Font::edges` 的返回值在两条路径上符号约定不同**：正常路径给两个正量（`bbox.y_max`、`-bbox.y_min`），
  而**没有字形包围盒时**（空格等）给的是带符号值（`-ascender`、`|descender|`）。第一版直接按
  `y - up / y + down` 组装，空格字形就产出了**上下颠倒的矩形**（块高为负、行序判断全乱）。
  统一规范化成"两个正值"后才正常。
- **行内构造不是块**：`$a^2+b^2=c^2$` 与 `` `行内代码` `` 起初被当成独立块，把段落切成三段
  （块级 widget 会截断段落）。现在 `Raw` / `Equation` / `Code` 只在**独占整行**时才算块。

**未覆盖的字符**是标记语法本身（`=`、`$`、`^`、反引号）与代码行 —— 它们不产生字形，属预期，
也正好对应"源码透镜"里保持源码显示的那些块。

## 十、未验证 / 未决（别当结论用）

- **Release 构建下的耗时未测**（阶段 0 的数字全部来自 debug 构建）；跨按键增量（World 复用 +
  `Source::replace`）的实测加速比同样未测。
- **脚注/浮动内容的切带规则**具体怎么写还没定（阶段 0 只证明了"按 span 求并集"会坏）。
- **块粒度**：嵌套列表项目前整个 `ListItem` 算一块（外层），是否要细到每一项未定。
- **诊断波浪线在切片上的落点**、以及"点击切片 → 光标落在正确字符"的精度（`jump_from_click`
  是字符级的，我们自写的帧遍历必须与它对拍）都还没做。
- `typst-ide = "0.15.1"` 加进本工程能否直接编译（依赖树里 `typst-html` / `indexmap` / `unscanny` /
  `serde` 都在，但版本对齐未实测）。
- `svg_in_html` 的 `Tracked<LateLinkResolver>` 具体写法未编译验证。
- "改 `viewBox` 做局部裁剪"在含 `clip-path` / 渐变 / `render_bleed` 的页面上是否 100% 安全未实测。
- comemo 增量（World 复用 + `Source::replace`）的实测加速比未知。
- 块切分的**粒度**取舍（整列表 vs 单列表项 vs 整表）对手感的影响未知。
- `TextItem::bbox()` 的 y 轴方向（源码注释说是 y-up 转 frame 方向有符号翻转）需实测确认。
- 多窗口 `FileId` 隔离现状需实际核对。

## 十一、来源

- 本地 vendored 源码：`~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/typst*-0.15.1/`
  （`typst` / `typst-library` / `typst-layout` / `typst-svg` / `typst-html` / `typst-syntax` / `comemo`）
- [typst-ide v0.15.1 `jump.rs`](https://raw.githubusercontent.com/typst/typst/v0.15.1/crates/typst-ide/src/jump.rs) · [crates.io: typst-ide](https://crates.io/crates/typst-ide)
- [typst HTML 导出手册](https://typst.app/docs/reference/html/) · [tracking issue #5512](https://github.com/typst/typst/issues/5512)
- changelog：[0.13](https://typst.app/docs/changelog/0.13.0/) · [0.14](https://typst.app/docs/changelog/0.14.0/) · [0.15](https://typst.app/docs/changelog/0.15.0/)
- 官方 web app（源码+预览、click-to-jump）：[export-and-preview](https://typst.app/docs/web-app/export-and-preview/) · [2023-03 更新](https://typst.app/blog/2023/march-update)
- 先行者：[flatflax/typst-editor `doc/architecture.md`](https://github.com/flatflax/typst-editor/blob/main/doc/architecture.md)（GPL-3.0）· [tinymist](https://github.com/Myriad-Dreamin/tinymist) · [typst-preview](https://github.com/Enter-tainer/typst-preview)（已归档）
- 本仓库既有调研：`docs/WYSIWYG-调研.md`（2026-09-10，公式内联渲染落地前后的记录）
