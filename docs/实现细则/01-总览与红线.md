# 实现细则 · 总览与红线

> 本册是 `CLAUDE.md` 总览部分（当前状态 / 红线 / 已知未决 / 协作偏好 / 文档地图 / 架构与模块清单）的详版：保留原始措辞、实测数据与踩坑经过。
> 规则以 `CLAUDE.md` 为准；索引见 [`README.md`](./README.md)。

Typst-pad：**仿 Typora 的 Typst 桌面编辑器，两套 UI**——「写作模式」（默认，单栏整页纸张：公式与标记就地排版、光标/选区进入即展开源码、无行号）与「源代码模式」（`Ctrl+E`，双栏：等宽代码编辑器 + 右栏整页预览）。前端 SvelteKit SPA（adapter-static），桌面壳 Tauri 2（Rust），编译渲染用**内嵌 typst crate**（0.15.x，Rust 进程内编译，本地字体）。纯本地运行：前端已无 wasm、无网络依赖（`@preview` 包按需下载），字体随包分发。代码注释与 README 均为中文。

## 交接须知（新同学先读这一节）

- **当前版本 `0.9.0`（2026-09-18 发布）**，主题是「写作模式变成真的 Typora 形态」：① **块级渲染第一次进发行版**（非光标块整块显示成引擎切片、光标所在块展开源码，点切片落到精确字符、可拖选跨块复制、`#link` 是可点热区）；② PR #60 审查的严重项与一般项（都落成下方「写作模式的块级渲染」里的红线）；③ 弹出的面板统一白底黑字、浮层条目浅灰；删掉「保存空文档」确认窗。**逐版历史看 `CHANGELOG.md`，提交脉络看 `git log`**——本文档只写"现在是什么样、别再踩哪些坑"。
- **发行状态**：`v0.9.0` 已发布并匿名验证过（`latest.json` 200 / version `0.9.0` / 安装包 16749934 字节 / minisign 验签通过，keyid `4e79c0dd71347bb5`）。一行复验：`node scripts/verify-release.mjs 0.9.0`（退出码 0 = 四步全过）。
- **自动更新只覆盖 Windows**（CI 只构建 Windows 安装包，`latest.json` 里也只有 `windows-x86_64`）；**仓库必须保持公开**——转公开是自动更新能工作的运维前提，改回私有客户端立刻全部「检查更新失败」。
- **发版数字由用户定**：上一轮他说「y 往后一个」，我把"main 上已经并了 10k 行新功能"这个岔口摆出来问了一句，他改成 0.9.0。再遇到"补丁位里躺着新功能"，照这个办法：**先说清楚这一版会带什么，再让他定数字**。

## 改动前的红线（都是踩过的，勿回退）

1. **改 `src/` 会 HMR 到正在运行的 `tauri dev`；改 `src-tauri/` 会让 Tauri 重启应用**（未保存内容靠"启动时恢复上次内容"兜住；排查"内容怎么变了"时先想这一条）。
2. `editorDoc` 必须是编辑器内容的**实时镜像**，绝不允许落后（否则丢未保存内容）。
3. `live-preview.ts` 里任何 `Decoration.*.range(a, b)` 都要 `b > a`；装饰计算与 widget 渲染都在 try/catch 里——**别把可能抛异常的代码挪到 try 之外**（抛进 CodeMirror 事务 = 编辑区卡死，用户报过）。
4. 写作模式的左右留白必须挂 `.cm-scroller`，**不能**是 `.cm-content` 的 `padding`（否则整行选区底色比文字列两边各宽 48px）。
5. 菜单（Alt / 鼠标）**不许夺走编辑区焦点**（用户明确要求："不要改变当前编辑位置"）。
6. `src-tauri/fonts/` 不放 `static/`（会被 SvelteKit 打进前端产物，安装包白胖 5.7MB）。
7. `vite.config.js` 的 **dev 白屏修复三件套**与 `optimizeDeps.exclude: ["codemirror-lang-typst"]` 不许删（删了 WSL/WebKit 下会白屏）。**注意**：前端 0.8.2 起已无 wasm（语法高亮走 `codemirror-lang-typst/lezer`），这两个 wasm 插件目前没有服务对象——要删得先在 WSL/WebKit 上验一遍启动。
8. 发版纪律：版本号三处一致（`package.json` / `src-tauri/tauri.conf.json` / `src-tauri/Cargo.toml`）；rust-cache 不许加 `cache-on-failure`。
9. **绝不阻塞等 GitHub workflow（用户 2026-09-14 连着强调三次）**：推 main、打 tag、执行后续命令**一律不等** CI / Release 跑完；**不要**挂"等 run 结束再执行"的后台轮询任务；报告状态最多**单次**查一次 `gh run list`（单查可以，轮询不行）。判断标准：**这一步会让回合停在"等外部流程"上 → 不许做**；`job_output(wait: true)` 等 CI/Release、`gh run watch`、轮询循环都算阻塞（犯过两次，用户发过火）。
   - 原有的「别在 CI 运行中 push main」**按用户指示作废**：`ci.yml` 的 concurrency 会 cancel 掉 main 上正在跑的 run（被 cancel 的 run 不保存 rust-cache），他明确接受这个代价、不接受为它停下等 CI。唯一保留的建议是"能一次推完的提交就一次推完"。
   - 边界：**一次性延时动作可以**——例如"构建完成后把草稿 Release 直接 Publish"，挂成后台 job（有界重试、不占回合）。
10. **自动更新的签名密钥不许动**：`tauri.conf.json` 里已有 `plugins.updater.pubkey`，所以任何 `tauri build`（含 main 的 CI）**必须**能拿到私钥——仓库 Secrets 的 `TAURI_SIGNING_PRIVATE_KEY` / `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` **删了就构建不了，换了就再也发不出更新**（老用户装了带旧 pubkey 的版本，只认旧私钥签的包）。本地打包同理（`TAURI_SIGNING_PRIVATE_KEY_PATH`）。
11. **发版权在用户手里：不许自行开新版本**（用户原话「等我说过了才能开新版本」）。改版本号三处、更新 CHANGELOG 的版本段、打 tag、建/发 Release——**这四件事都只在用户明确说"发 X.Y.Z"之后才做**；"这版攒了几个修复，我觉得该发了"不是理由。拿不准时**问一句**，比先斩后奏强。

## 已知未决 / 可做（都不是 bug，是留给接手人的选择）

- 浏览器开发模式（`?browserdev=1`）的编译是**假实现**（内存里的假文件系统 + 假 SVG）。想在浏览器里看真实排版走 `fixtures:math` / `fixtures:blocks` 夹具链路；**真保存 / 导出 PDF / 系统对话框必须桌面版**。
- 设置弹窗的「启动时恢复上次内容」默认开；若不想让新用户被上次内容打扰，可改默认或加提示。
- **自动更新只覆盖 Windows**：要上 macOS/Linux 得先补构建 job，并给 `+page.svelte` 的安装成功分支接 `tauri-plugin-process` 的 `relaunch()`（Windows 上 NSIS 装完会自己把应用拉起来，无需 relaunch，所以现在没引这个插件）。
- 更新检查**没有**"忽略这个版本"的粒度：用户点一次「稍后」，**以后任何新版本都不会再自动弹窗**（这是用户明确要的）；状态栏「可更新到 vX」入口与手动检查不受影响。若想要"只有更新的版本才再提示"，得再存一个 `skippedVersion` 比大小。
- 写作模式：**脚注正文（页底装饰）不显示**（夹紧后落在所有带之外；要显示得单独切一块"页底装饰"，它没有源区间，尚未做）；**表格是整块**（按行切会把表格线切开，有意不做）；浏览器查找 / 拼写检查 / 无障碍拿不到（没有每字形文字层）；**真机（tauri dev）手感与性能尚未系统验证**。

## 和这位用户协作的偏好（实测经验）

- 他会**自己跑桌面版**验证（WSL 里 `npm run tauri dev`），反馈通常是一句话现象或一张截图；**先复现、再改**——复现不出来时加防御性兜底 + 让错误可见（状态栏），别硬猜。现象只在真机 WebView2 上出现时，Chromium 验收复现不了，要按红线 9 之外的办法处理（模拟引擎 / 让数字可见）。
- **他不喜欢等慢测试**：日常改动跑 `npm run check` + 相关单测（几秒级）就够；`scripts/browser-check/`（约 1 分钟）只在改到编辑器/装饰/布局这类易回归的地方才跑，而且不必盯着结果等它绿。
- **绝不阻塞等 GitHub workflow**（红线 9 的全文，别再犯）。
- **发版两步要分清**：① **要不要发、发哪个版本号，由他说**；② 一旦他说了要发，**草稿 Release 一建好就直接 Publish，不用再问**（草稿资产客户端拿不到、自动更新等于没上线）。
- **重大外部动作先问一句、机械动作别问**：仓库从私有转公开、放宽分支规则这类**不可逆或改仓库设置**的事先确认；"打完 tag 后把草稿 Publish""删掉临时文件"这类**由既有约定覆盖**的动作直接做。
- **他的机器上有他自己的东西：清理现场时只动自己创建的对象**（犯过：`taskkill /IM chrome.exe /F` 把他在 Windows 上开着的所有 Chrome 一起杀了，他立刻发火）。收尾只杀自己启动的进程（记 PID）、只删自己建的文件，别碰用户正在用的程序、窗口、端口与编辑器会话。
- 推送被规则拦住时先问一句"是要放宽规则还是走 PR"，别默认改成 PR 工作流、也别偷偷绕（他要的是"能直推 main"）。
- 明确的产品偏好：仿 Typora 的观感（**不要工具条**、菜单 + 快捷键）；"不要改变当前编辑位置"（Alt/菜单不许夺焦）；界面不要出现多余色块与凸出（选区底色要对齐文字列）。

## 文档地图

| 想了解 | 看哪节 |
| --- | --- |
| 整体架构与模块清单 | 架构（含配置与辅助目录） |
| 编译链路 / 诊断 / 导出 PDF / 项目根与 `#import` | 编译数据流（核心链路） |
| 写作模式的"真实 typst 排版"、切片与源码透镜 | 写作模式的块级渲染 + `docs/文档模式渲染保真-调研.md` |
| 所见即所得怎么实现、有哪些坑 | 所见即所得（编辑器内联渲染）数据流 |
| 自动更新怎么工作、密钥怎么管、发版要注意什么 | 自动更新（tauri-plugin-updater）数据流、CI / 发布约定 |
| 字体从哪来、为什么不放 static | 字体、原生编译后端 |
| 文件操作与路径安全 / 会不会清空文件 | 文件操作与路径安全 |
| 怎么跑验收、验收到什么程度 | 测试 |
| 处理别人交来的 PR 怎么审（**只有 PR 才走这一套**） | 代码审查：只在"处理 PR"时做这一套 |
| 本机 WSL 特有的坑（端口、SSH、headless Chrome） | 环境备忘（本机 WSL） |
| 这条块级渲染线的详细交接 | `docs/写作模式块级渲染-现状与交接.md` |

## 架构

```
# 前端：页面 / 布局（src/routes/）
src/routes/+page.svelte                  # 唯一页面：全部 `$state` 与调度中枢（**文件生命周期 / 公式队列 / 设置草稿与字体列表 / 启动恢复 / open-file 接球已外包**；
                                         #   页面只剩注入、菜单/编译/持久化/快捷键/缩放/更新）
src/routes/+layout.ts                    # SPA 模式（ssr = false），配合 adapter-static 的 index.html fallback

#   core/ —— 底层：纯逻辑 / 引擎 / 叶子工具 + 少数"依赖全注入的编排器"（document-session、zoom-controller、
#              math-queue、font-list）。
#              **规则：core 不 import 其它三层**
src/lib/core/startup-timing.ts           # 启动打点：首次编译完成后输出 [startup] 报告
src/lib/core/debug.ts                    # 调试日志通道 dbg（dev 默认开；--debug / ?debug=1 / localStorage 可开）
src/lib/core/typst-lex.ts                # 源码区域扫描：markup / code / raw / comment / string（标记识别的前提，纯函数）
src/lib/core/math-ranges.ts              # 公式范围扫描（$...$ / $ ... $）+ 缓存键 + 选区判定（选中整个公式不展开，纯函数）
src/lib/core/math-context.ts             # 公式编译上下文：前缀 + 文档内单行顶层 #let 定义（纯函数）
src/lib/core/markup-ranges.ts            # 常用标记拆解（标题/粗体/斜体/行内代码/围栏代码块/列表符号/链接，纯函数）
src/lib/core/typst-engine.ts             # 编译引擎：Tauri invoke 包装（compile_doc/compile_blocks/block_hit_test/compile_math/export_pdf）+ 结构化诊断
src/lib/core/file-ops.ts                 # Tauri dialog + invoke 封装（isTauri() 门控）
src/lib/core/document-session.ts         # 文档生命周期：打开 / 保存 / 重新读取 / 新建（脏文档必问、写盘唯一入口；依赖全注入）
src/lib/core/persistence.ts              # localStorage（key: "typst-pad:state"）
src/lib/core/session-restore.ts          # 启动恢复计划：存档 → 要落的状态（主题只认合法值、副窗口/空白内容/开关关掉都不恢复内容）
src/lib/core/open-file-claim.ts          # `open-file` 广播的接球规则：有焦点的窗口接（并取空队列）/ 没焦点时主窗口延迟兜底 / 副窗口不抢 / 启动时就绪后只主窗口取一次
src/lib/core/new-window.ts               # 新建窗口的规则：label 唯一（editor-<时间戳>）/ 前缀必须与 capabilities 的 editor-* 一致 / 失败报到状态栏（依赖全注入）
src/lib/core/window-events.ts            # 窗口级事件两条规则：拖放（只认 .typ、没给路径不提示）/ 关闭确认（按内容判定，空文档不拦）
src/lib/core/pdf-export.ts               # PDF 导出文件名推导（纯函数可单测）
src/lib/core/diagnostics-utils.ts        # 编译源位置 → 文档位置映射（mapCompiledPosToDoc）与波浪线区间（squiggleRanges）
src/lib/core/doc-utils.ts                # 文档纯函数：isBlankDoc / isEffectiveDirty / ensureTrailingNewline / fileNameOf / UNTITLED_TITLE
src/lib/core/write-commands.ts           # 写作模式格式命令的纯逻辑：planForCommand → EditPlan（不碰 CodeMirror）
src/lib/core/preview-scale.ts            # 预览缩放纯逻辑：按栏宽重排 + 等比缩放回退（画布恒 ≤ 栏宽）
src/lib/core/zoom.ts                     # 界面缩放纯逻辑：档距/方向/上下限收敛/浮点圆整/滚轮余量累积（纯函数）
src/lib/core/zoom-controller.ts          # 缩放编排器：把"用户要这一档"变成"引擎接受了"（复核/沉降窗口/失焦重设，依赖全注入）
src/lib/core/updater.ts                  # 自动更新包装层：check → 可判别结果、下载进度事件流、句柄释放（只包 Tauri）
src/lib/core/update-utils.ts             # 自动更新纯逻辑：启动检查延迟 / 进度换算 / 错误文案 / 稍后抑制
src/lib/core/update-flow.ts              # 更新流程状态机：可判别联合 UpdateFlow + 各步计划（纯函数）
src/lib/core/app-settings.ts             # 应用设置的纯模型：生效配置 ↔ 设置弹窗草稿（复制/比对/保存后状态栏文案）
src/lib/core/font-settings.ts            # 正文字体设置：内置族列表 + buildFontFamilies（拉丁基准必须留最前）
src/lib/core/font-list.ts                # 字体下拉的数据源：按目录扫描（归一化）/ 添加目录 / 移除目录（依赖全注入）
src/lib/core/font-warnings.ts            # 字体相关警告的中文提示
src/lib/core/failure-text.ts             # 失败文案：从 invoke 抛出的东西里抠一句原因
src/lib/core/clipboard.ts                # 写剪贴板：execCommand 主路径 + navigator.clipboard 兜底
src/lib/core/script-errors.ts            # 脚本异常兜底文案：把 error/unhandledrejection 变成状态栏一句人话
src/lib/core/block-offsets.ts            # 字节偏移 ↔ CodeMirror 位置（UTF-16）换算（CJK/emoji 直接当位置用会整篇错位）
src/lib/core/block-plan.ts               # 块级渲染的**规划**纯逻辑：格子覆盖 / 展开哪一块 / 窗口外沿用 / 竖直移动落点 / 编辑后重映射
src/lib/core/block-state.ts              # 块表的**落地编排**：编译结果 → 新块表（成功/失败两条分支）/ 编辑期间平移 / 视口 → 编译窗口（字节）
src/lib/core/block-hit.ts                # 点击定位的坐标纯逻辑：切片内的点 → 页面坐标（pt）+ 命中结果钳回块内

#   editor/ —— CodeMirror 视图层 + 键盘语义（依赖 core）
src/lib/editor/app-keys.ts               # 页面级按键路由纯函数（Esc / Alt+Z / Ctrl+Shift+N / Ctrl+Shift+格式 / Ctrl+R / Ctrl+W，顺序敏感）
src/lib/editor/Editor.svelte             # CodeMirror 6 封装：受控 doc、主题 Compartment、诊断波浪线、所见即所得接线、写作模式样式
src/lib/editor/editor-keymap.ts          # CM 键位：回车继承缩进、空 `$` 配对整对退格、注释等
src/lib/editor/live-preview.ts           # 所见即所得 CM6 扩展的**组装层**：StateField + 拖选 + 翻页/竖直移动 + 渲染请求插件 → 一个 Extension
src/lib/editor/live-preview/             # 拆出去的部件：options / widgets / markup·math·block-decorations / covered / theme / **block-drag**（切片拖选）/ **block-moves**（竖直移动与翻页）/ **requests**（渲染请求）
src/lib/editor/math-queue.ts             # 公式渲染队列：去重 + 120ms 防抖 + 缓存（上限 500 条）+ 失败退回"仅前缀"上下文（依赖全注入，可单测）
src/lib/editor/typst-highlight.ts        # 压掉编辑器默认高亮给标题加的下划线
src/lib/editor/word-wrap.ts              # 源码模式 Alt+Z 折行的判定与文案（排除 Ctrl+Z）
src/lib/editor/auto-pair.ts              # 输入 `$` 的自动配对纯逻辑
src/lib/editor/auto-indent.ts            # 缩进纯逻辑：INDENT_UNIT（一档 = 4 空格）+ 回车继承上一行缩进
src/lib/editor/editor-font.ts            # 写作模式打包字体：Rust bundled_font → FontFace 注册，字体栈与 typst 默认族同序
src/lib/editor/scroll-anchor.ts          # 滚动锚定：把"光标该落在屏幕哪个高度"变成 CM 的 scrollIntoView 目标

#   ui/ —— Svelte 组件 + 展示层模型（依赖 core）
src/lib/ui/MenuBar.svelte                # 菜单栏（Alt 激活 + 字母快捷键；**不夺编辑区焦点**）
src/lib/ui/ContextMenu.svelte            # 自定义右键菜单 UI（命令映射在 +page.svelte）
src/lib/ui/menu-keys.ts                  # 菜单栏按键决策纯函数（Alt / accessKey / Ctrl+单键匹配）
src/lib/ui/context-menu-utils.ts         # 右键菜单纯逻辑：区域判定 / 菜单项 enabled / 弹出位置收边
src/lib/ui/popover-utils.ts              # Popover 位置收边（视口内夹紧）
src/lib/ui/error-list.ts                 # 错误/警告列表组装 + 前缀行定位 + 复制文本格式化（纯函数）
src/lib/ui/update-notes.ts               # 更新说明的 Markdown 渲染（受控子集 → 安全 HTML）

#   dev/ —— **只在开发/验收链路里用**：`?browserdev=1` 时由 `src/app.html` 动态加载的浏览器桩 + 假产物生成器（不被任何生产模块 import）
src/lib/dev/browser-dev-stub.ts          # 浏览器开发桩：假 __TAURI_INTERNALS__ + 各模拟开关 + 命令路由/安装
src/lib/dev/browser-dev-stub/            # 桩的假产物生成器：fake-layout（假整页 SVG）/ fake-math（假公式 + 注入真产物）/ fake-blocks（假切片 + 粗略点击定位）
src-tauri/src/lib.rs        # Rust 壳，只留**模块声明 + 应用装配**（插件、setup、IPC 命令表、事件循环）
src-tauri/src/file_commands.rs    # 读/写文件命令 + 待打开队列（PendingFiles / 首次启动参数解析 / macOS queue_open）
src-tauri/src/dir_listing.rs      # list_dir_typ：递归列 .typ（深度 ≤ 8、≤ 500 个、符号链接目录不递归）
src-tauri/src/compile_commands.rs # CompileState + **编译通道**（in_compile_channel：一把互斥锁 + spawn_blocking）+ compile_doc/compile_blocks/block_hit_test/compile_math/export_pdf
src-tauri/src/font_commands.rs    # default_font_families / bundled_font（raw IPC）/ list_font_families
src-tauri/src/paths.rs      # 路径安全校验：validate_typ_path / validate_write_path / absolutize / is_typ_file（唯一闸门）
src-tauri/src/startup_timing.rs   # 启动打点（仅 debug 构建；`[startup] rust phase:`）
src-tauri/src/block_geometry/  # 块级渲染：源块划分 + 帧遍历（字形 Span → 源字节区间）+ 按 y 序切带 + 切一块渲成 SVG + 点击命中（pick_hit / HIT_CACHE）+ 文档正文字号
src-tauri/src/packages.rs   # 包系统：@local 读取 / @preview 自动下载缓存（目录规范与 CLI 一致 + 安全解压）
src-tauri/src/typst_world/   # 内嵌编译世界：字体加载（FontBook）/ 相对 include 磁盘解析 / 项目根放宽 / 诊断转换（SVG/PDF）
src-tauri/src/main.rs       # 桌面入口（调用 lib.rs 的 run）
src-tauri/fonts/            # 打包字体（见「字体」：**不放 static/**）
```

配置与辅助目录：

- `vite.config.js`：SvelteKit + wasm 插件 + **dev 白屏修复三件套（`syncWasmInit` 等，勿动）** + `bundledFontsDev`（**只作用于 dev**：把 `src-tauri/fonts/` 借 `/__bundled-fonts/*` 暴露给浏览器验收用，真机走 Rust 的 `bundled_font`；白名单挡路径穿越）；Tauri 开发用 `TAURI_DEV_HOST`；`server.port: 1420` + `strictPort`。
- `vitest.config.ts`：jsdom + `include: ["src/**/*.test.ts", "scripts/**/*.test.mjs"]` + `server.fs.allow: [".."]`（scripts 是普通 JS，不参与 svelte-check）。
- **格式门**（2026-09-19 补，见「CI / 发布约定」）：`.prettierrc.json`（printWidth 100 + `prettier-plugin-svelte`——Svelte 5 runes **必须**用它，否则 `.svelte` 被当 HTML 解析、`$state`/`{#if}`/`class:` 会被破坏）、`.prettierignore`（`docs/` 与 `.github/` 刻意排除：前者的表格列宽、后者的注释对齐都是手写的）、`.editorconfig`。Rust 用 **rustfmt 默认风格、不加 `rustfmt.toml`**（试过 `use_small_heuristics=Max` 想少展开一点，diff 反而从 112 处涨到 119 处）。
- `svelte.config.js`：`@sveltejs/adapter-static`（SPA，`fallback: index.html`）。
- `src-tauri/app-icon.svg`：**应用图标的源文件**（矢量 1024×1024，「叠纸 + T」）。改图标只改它，然后 `npm run tauri icon src-tauri/app-icon.svg` 重新生成 `src-tauri/icons/` 全套，favicon（`static/favicon.png`）同源导出。**两个坑**：① `tauri icon` 会顺带产出 `android/`、`ios/`，本项目只做桌面端，**生成后删掉**；② 图标是打包时嵌进 exe 的，装了的用户要重装才看得到。
- `.github/workflows/`：`ci.yml`（test + build-bundles）、`release.yml`（tag 发草稿 Release），见「CI / 发布约定」。
- `scripts/`：`check-fonts.mjs`、`download-fonts.mjs`、`verify-release.mjs`、`generate-latest-json.mjs`、`browser-check/`（CDP 验收）、`install-vs-buildtools.bat` / `verify-app.bat`（Windows 辅助）。
  - `capabilities.test.mjs`：**Tauri capability 静态体检**（前端用到的插件命令 → 必须在 `src-tauri/capabilities/default.json` 里有对应权限；ACL 拒绝只在真机运行时才出现）。
  - `editor-fonts.test.mjs`：**打包字体的静态对齐**（Rust `EDITOR_FONT_FILES` ↔ 前端 `EDITOR_FONT_FACES` ↔ `WRITE_FONT_STACK` 族名顺序 ↔ `src-tauri/fonts/` 里真有这些文件 ↔ `bundle.resources` 会分发它们）。
- `docs/`：`WYSIWYG-调研.md`、`文档模式渲染保真-调研.md`（块级渲染的调研与实测数据）、`写作模式块级渲染-现状与交接.md`；`CHANGELOG.md` 按 Keep a Changelog 维护；`.browser-check/` 为验收产物（已 gitignore）。

