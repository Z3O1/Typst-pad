# 写作模式块级渲染 —— 现状与交接

> 这份文档只记**不在版本库里、只在会话里**的信息：两个工作区怎么摆、这条线到哪一步了、
> 验收数字是多少、还剩什么、怎么跑。
> 实现细节与逐条红线**不在这里重复**，看 `CLAUDE.md` 的「写作模式的块级渲染」那节
> （完整版，含每条坑的实测数据）与 `docs/文档模式渲染保真-调研.md`（741 行）。

最后更新：2026-09-18（**这条线已通过 PR #60 合并进 `main`**：merge commit `071cd1d`，
`main..feat/block-geometry` 现在是空的；`main` 之后又走了几个提交。**但还没进过任何发行版** ——
最新 tag 仍是 `v0.8.4`（在合并之前发的），没有任何 tag 含本线的提交，版本号三处也仍都是 0.8.4）

---

## 1. 工作区布局（2026-09-16 起）

```
/home/z301/Typst-pad                  [main]  ← 普通目录 = 发布线（0.8.4 已发布）
/home/z301/Typst-pad-block-geometry   [main]  ← 本目录 = 块级渲染线（已随 PR #60 合并进 main）
```

- 两个目录现在**都在 `main` 上**（本目录原来那条 `feat/block-geometry` 也还留在本地与 `origin`
  上，停在 `2684eb8` = 合并进来的那个 tip）；具体到哪个提交看 `git log --oneline main`（会一直往前跑，
  所以这里不钉提交号）。

- **本目录从 2026-09-16 起有自己的 `.git`（独立仓库，20M）**。原先它是 `/home/z301/Typst-pad`
  的一个 **worktree**、共用那份 `.git`，但共享的 git 目录在工作区之外 —— 提交/写 index 会被文件
  沙箱拦成 `Read-only file system`（`Unable to create '…/.git/worktrees/…/index.lock'`）。
  所以 `cp -a` 了一份 `.git` 过来（连同全部历史、分支、tag、`origin` 与 remote-tracking ref、
  `info/exclude`），删掉 `worktrees/` 与那份属于主目录的 index，HEAD 指到 `feat/block-geometry`，
  `git reset --mixed` 重建索引 —— `git status` 干净、`git log` / `git tag` / upstream 全都照旧。
  - 两个目录现在是**两个独立仓库**：在本目录提交完全不影响主目录，**同步只能靠 push / pull**
    （`feat/block-geometry` 已经合并进 main，本地与 `origin` 都停在 `2684eb8`）。
  - 主目录那边**还留着一条指向本目录的 worktree 记录**（主目录 `git worktree list` 里会显示它，
    清掉就在主目录跑 `git worktree prune` —— 只删元数据、不碰本目录的文件）。留着也无害，
    只是主目录里 `git checkout feat/block-geometry` 会被"已在另一处检出"挡住。
- 远端：`origin` = `git@github.com:Z3O1/Typst-pad.git`。**2026-09-16 已把 main（`a35dadf`，含 `0.8.0`
  发布 / 缩放只归用户的修复 / 两道丢内容防护 / 新图标）合并进本分支**（三个冲突：`+page.svelte`
  的 `remapBlocksForEdit` 与 main 的注释保留双方、`wysiwyg.mjs` 两边的新用例都留（本线那组编到
  第 40 组）、`CLAUDE.md` 的计数与命令取并集）。**反过来那一步后来也做完了**：2026-09-18 本线通过
  PR #60 合并进 `main`（merge commit `071cd1d`，之后 `main` 上还有别的提交）。
- **`src-tauri/target` 现在是本目录自己的真目录（2026-09-16 起，用户：「把缓存在当前目录也建立一份」）**：
  此前它是软链、指向 `/home/z301/Typst-pad/src-tauri/target`（31G 共享缓存），代价有两个 ——
  ① 两个目录同时 `cargo build` / `tauri dev` 在同一把文件锁上互等；② 那个路径在**文件沙箱之外**，
  `cargo test` / `npm run fixtures:blocks` 一律被拦成 `Read-only file system`（要跑就得先要一次授权）。
  现在软链已删、`target/` 是本地真目录：**第一次构建是冷的**（typst + tauri 全套，十几分钟起步，
  占 ~10G），之后就是本目录自己的增量缓存；好处是 cargo 那一套（含 `fixtures:blocks`）**不用再要授权**、
  也不再和主目录抢锁。主目录那份 31G 缓存没动（它的 `tauri dev` 照旧）。
- 忽略规则：`src-tauri/.gitignore` 里有 `/target/`（**只匹配真实目录**），所以本地真目录天然被忽略；
  软链时代为了它另在 `.git/info/exclude` 里加过一行 `src-tauri/target`，那行现在冗余（留着无害）。
- 本目录 `node_modules` 是独立装的（199M，`npm install` 154 个包 / 6s），
  `.svelte-kit` 已 `svelte-kit sync` 过，`npm run dev -- --port 1426` 冒烟过（HTTP 200 / ready 934ms）。

## 2. 这条线到哪一步了

写作模式 = **渲染表面 + 源码透镜**：非光标块显示成 typst 引擎自己画的那一块 SVG 切片，
光标所在块展开成源码。阶段 1「能看」、阶段 2「能用」（点击精确字符 / 滚动锚定 / 翻页 / 出错不整篇退回）、
阶段 3「好选好用」（切片上拖选跨块 + Ctrl+C、`#link` 可点、整块选中保持切片外观）**都已完成**。
光标移动（↑/↓/PageUp/PageDown）**已改成与代码模式同语义**（用户 2026-09-16 的要求）。

下表列的是这条线的**功能提交**（合并之后 `git log --oneline main..feat/block-geometry` 已经是空的，
那个数不再有意义；全部提交看 `git log --oneline main`，因为这条线已经并进 main 了）：

| 提交 | 内容 |
| --- | --- |
| `2cd243c` | 调研文档（能否和真实 rust typst 一样渲染） |
| `2a6a371` | 阶段 0 探针：源块 ↔ 版面区域的几何映射 |
| `8554879` / `13bd566` | 阶段 1：块级渲染落地 + 计划表状态 |
| `6241c51` | 块切片必须**整行对齐**（否则 CM 既插 widget 又保留原文） |
| `8bb96a0` | 切片几何等价验收（真实产物）+ 窗口化回归网 |
| `e3cc654` | 块带**夹紧**（脚注把后面几块压扁 → 重复渲染）+ 场景验收 |
| `98647e5` | 分块判据跟 typst 语义走（`$x$` 是行内公式，不打断段落） |
| `93651da` | 竖直移动不再跳回文档开头（用户报「`== 6` 前面按上跳开头」） |
| `b4b3dbf` / `e0efce4` | 阶段 2：点击精确字符 / 翻页 / 滚动锚定 / 编译失败不作废 |
| `5f7dbcd` | 块内按 Enter 之后正文被旧切片吞掉（用户报「在一块内 Enter 插入块的时候会有问题」） |
| `f9f67b1` | 各种输入的验收：12 个编辑动作（浏览器）+ 68 项编辑矩阵（纯逻辑） |
| `022c715` | 阶段 3：切片上拖选跨块 + Ctrl+C、`#link` 可点 |
| `2f4dc43` | 验收脚本的 CDP 导航自带重试 |
| `ea8d5ae` | **打字期间不编译**（用户报「输入手感很差」）+ 整块选中不展开（围栏自动隐藏） |
| `1150c1d` | 选中整个公式不展开（用户要求） |
| `6bb2775` | 标题字号梯度对齐 typst（1.4/1.2/1.0em）+ 「被选中」观感改造（用户截 Ctrl+A 说「太丑了」） |
| `2bd7c97` | 没有输出的块整格隐藏（`#set` / `#show` / `#let` / 纯注释行） |
| `62ee9c6` | 行尾 Enter 拆分块之后光标飘到正文列最右（空行归上一块） |
| `2e86f7b` | **源码透镜跟随文档字号与行距**（用户报「不要光标在哪里哪里就变大了」） |
| `a07f3ad` | **光标移动 = 代码模式的语义**（用户：「我希望光标移动和代码模式的光标移动一样」）：↑/↓ 逐源码行走（空行也停、列保留、落点在切片里就展开那一块）、Shift+↑/↓ 一并接管、翻页距离对齐 CM 的 `pageInfo` 且不再按滚动余量夹位移；旧的"一次跨一整块"（`verticalBlockTarget`）删除 |
| `8329b78` | 本目录改成**独立 `.git`**（不再是共享 `.git` 的 worktree；提交不再被文件沙箱拦） |
| `2d2c356` | **合并 main**（`a35dadf`，含 0.8.0 发布 / 缩放修复 / 两道丢内容防护 / 新图标） |
| 本轮 | **手感/细节三件**：① **写作模式的源码透镜装上打包字体**（Libertinus Serif + 思源宋体子集，Rust `bundled_font` → FontFace；字号/行高/字体三条腿齐）② **失败文案带上 Rust 原因**（`failure-text.ts`：保存失败：仅支持 .typ 文件）③ 删 6 个 wasm 时代死脚本 + `package-lock.json` 版本字段对齐 0.8.0；另把 `src-tauri/target` 换成本目录自己的真缓存 |

最后一条的实测前后（600px 视口 + 真实夹具，比例 1.32 ≈ 真机 1.333）：

| 场景 | 修前 | 修后 |
| --- | --- | --- |
| 中文长段落 光标进出块的页面高度差 | +22px | **0px** |
| 标题层级 | +19px（照抄 typst 间距时 +40px） | **+3px** |
| 代码与表格 | +45px | **+8px** |
| 源码字号 / 切片字号 | 1.091 | **1.000** |

字号链：Rust `block_geometry::document_text_pt` 按**字符数取众数**量出文档正文 pt（默认 11pt），
随 `compile_blocks` 的 `textPt` 返回 → 前端挂成 `--write-doc-px`（pt × 4/3）→
`.cm-editor` 字号取它、`.cm-content` 行高取 typst 的 `par.leading`（1.65）。
默认文档 = **14.6667px / 24.2px 行盒**；写了 `#set text(size: 12pt)` 的 = **16px / 26.4px**。

## 3. 验证状态（最新一版：合并进 `main` 之后）

> **更新（2026-09-19，PR #62 合并后）**：`npm run check` 现在是 **0 errors / 0 warnings**（历史遗留的 `previewHost` 警告随"预览栏拆成 `PreviewPane.svelte` + 句柄 `paper()/body()`"消失，`previewHost` 这个变量在生产代码里已不存在），`npm test` 是 **45 文件 / 834 项**。下面 2026-09-18 那张表是当时的快照，保留原样备查。

**最新一版复验（2026-09-18，PR #60 合并进 `main` + main 的 0.8.4 之后；本机实测）**：

| 套件 | 结果 |
| --- | --- |
| `npm test`（vitest / jsdom） | **710 项通过**（38 个文件） |
| `cargo test --manifest-path src-tauri/Cargo.toml` | **61 passed / 6 ignored** |
| `npm run check`（svelte-check） | **0 errors / 1 warning**（历史遗留的 `previewHost`） |
| `scripts/browser-check/wysiwyg.mjs` | **290 项** |
| `scripts/browser-check/writing-blocks.mjs` | **133 项** |
| `writing-blocks-visual.mjs` | **76 项** |
| `writing-blocks-hit.mjs` | **27 项 / 136 次点击全中** |
| `writing-mode-scenes.mjs` | **64 项** |
| `wysiwyg-visual.mjs` | **20 项** |

**上一版复验（2026-09-18，把 main 合到 `2c8e50e`（0.8.1/0.8.2/0.8.3）之后）**：
> 这一轮合并带进来三件事会碰到本线：① **模式切换键从 `Ctrl+/` 改成 `Ctrl+E`**（`Ctrl+/` 归注释）
> —— 本线的两套验收脚本还在按 `Ctrl+/` 切模式，合并后立刻红了 3 条，已改成 `Ctrl+E`；
> ② 语法高亮换成无 wasm 的 `codemirror-lang-typst@0.6`（`npm install` 必须重跑，**本机要把 npm
> 缓存指到工作区内**：`npm install --cache "$PWD/.npm-cache"`，否则 `~/.npm` 会被文件沙箱拒成 EROFS）；
> ③ `wysiwyg.mjs` 两边都新增了用例 —— main 的第 40/41/42 组原样保留，本线那组编到 **43** 并接在其后。

| 套件 | 结果 |
| --- | --- |
| `npm test`（vitest / jsdom） | **693 项通过**（38 个文件） |
| `cargo test --manifest-path src-tauri/Cargo.toml` | **56 passed / 6 ignored**（main 的 import-root 那批 + 本线的） |
| `npm run check`（svelte-check） | **0 errors / 1 warning**（历史遗留的 `previewHost`） |
| `scripts/browser-check/wysiwyg.mjs` | **252 项**（main 243 + 本线「选中整个公式不展开」9 项） |
| `scripts/browser-check/writing-blocks.mjs` | **115 项** |
| `writing-blocks-visual.mjs` | **75 项** |
| `writing-blocks-hit.mjs` | **136/136 次点击全中**（27 项） |
| `writing-mode-scenes.mjs` | **64 项** |

**再上一版复验（2026-09-16，合并 main `a35dadf` + 手感/细节三件之后；Rust 已能本地构建）**：

| 套件 | 结果 |
| --- | --- |
| `npm test`（vitest / jsdom） | **677 项通过**（38 个文件：本线竖直移动 12 条 + main 那批 + 本轮 `editor-font` 7 条 / `failure-text` 8 条 / `scripts/editor-fonts.test.mjs` 6 条） |
| `cargo test --manifest-path src-tauri/Cargo.toml` | **48 passed / 6 ignored**（本目录自己的缓存，冷编 2m53s；含本轮新增的字体白名单用例。**修掉了一条本来就 flaky 的**：`hit_test_on_real_layout_maps_edges_to_block_bounds` 曾"单独跑绿、跑全集红"，原因是命中几何是进程级全局、被并行用例互相覆盖，现在读写缓存的用例都串行） |
| `npm run check`（svelte-check） | **0 errors / 1 warning**（历史遗留的 `previewHost`） |
| `scripts/browser-check/wysiwyg.mjs` | **232 项**（main 223 + 本线「选中整个公式不展开」9 项） |
| `scripts/browser-check/writing-blocks.mjs` | **115 项**（第 8 组"竖直移动 = 代码模式"14 项、第 10 组两端 3 项、**第 17 组"打包字体真装上"6 项**） |
| `writing-blocks-visual.mjs` | **75 项**（真实产物几何等价 + 链接热区；字体改动后仍全绿） |
| `writing-blocks-hit.mjs` | **136/136 次点击全中**（27 项） |
| `writing-mode-scenes.mjs` | **64 项**（含"光标进出块时页面不许变高"四条 —— 字体对齐后仍达标） |

> 合并 main 那一版（同一天早些时候）的旧数字：`npm test` 656 / `wysiwyg` 232 / `writing-blocks` 109；
> Rust 那次没重跑（共享 target 在沙箱外），本目录换成自己的缓存后才跑成 48/6。
| `scripts/browser-check/writing-blocks-visual.mjs` | **75 项**（几何等价 + 链接热区） |
| `scripts/browser-check/writing-blocks-hit.mjs` | **136 / 136 次点击全中**（27 项） |
| `scripts/browser-check/writing-mode-scenes.mjs` | **64 项**（含「光标进出块时页面不许变高」四条） |
| `scripts/browser-check/wysiwyg-visual.mjs` | **20 项** |

截图证据：`.browser-check/lens-doc-size.png`（光标所在段是源码、上下是切片，肉眼无接缝）。
`.browser-check/` 已 gitignore。

## 4. 还没做的 / 未决

**要先做的一件事：真机复验（`npm run tauri dev`，WebView2）** —— 上面所有数字都来自
headless Chromium + 夹具，真机手感尚未验证。该看的七条：

0. **↑/↓ 的手感**（本轮改动的落点）：逐行走时会不会觉得"段落之间多停一拍"（那是代码模式的行为）、
   走未展开的块时列保留得对不对（x 差 ≤16px 是验收线）、Shift+↑/↓ 扩选是否顺
1. 公式里打字的手感（去抖 150ms + 公式先跑 240ms 是否够；实测 12 个字符 → 1 次 `compile_blocks` + 1 次 `compile_math`，帧间隔最大 26ms）
2. 切片上拖选跨块 + Ctrl+C 复制
3. `#link("https://…")[文字]` 点击
4. 整块选中的淡色底（`#d7d4f0` 同款，**不要描边**）
5. `#set` / `#show` / 纯注释行整格隐藏（以及注释是否要跟 `#set` 一样藏起来 —— 用户可能想看见注释）
6. 行尾 Enter 拆分块之后的光标位置
7. **光标进出各块时字号/行距不该有变化**（`2e86f7b` 的目标）

**已知的固有差异（不是 bug，是"源码透镜"的代价，Typora 也一样）**：

- 段落间距：typst `par.spacing` = 1.2em，而源码形态里用户自己敲的空行 = 1.65em；
  两段同时展开时会有约 0.45em 的差。
- **源码比渲染长的块**（表格源码 5 行 vs 渲染 2 行）展开时必然变高 —— 实测 +8px 就是这类。
- 切片上没有文字层：浏览器查找 / 拼写检查 / 无障碍拿不到（取舍见调研文档第十二节）。
- **被藏起来的标记会让落点比代码模式"靠右几个字符"**：写作模式把标题的 `= `、列表的 `- ` 等标记
  藏掉了，位置 0~2 落在被藏的那一段里，所以从空行往上按 ↑ 落到标题行时位置是 2（不是 0）——
  视觉上都是"行首"，验收里那条断言写的是"第 1 行且列 ≤ 2"。

**已记在 CLAUDE.md「已知不足」里的**：脚注正文（页底装饰）在写作模式不显示；
表格仍是整块（按行切会把表格线切开）；真机手感/性能未验证。

**`package-lock.json` 的版本字段已对齐**：它现在与 `package.json` / `src-tauri/Cargo.toml` /
`src-tauri/tauri.conf.json` 一样是 `0.8.4`（当初记过一条"它还是 `0.7.3`"，那是发版时漏同步，
合并 main 时已经顺手改掉）。

**合并做了、发版没做**：这条线**已通过 PR #60 合并进 `main`**（merge commit `071cd1d`），
但**没有打 tag、没有动版本号、没有发版** —— 最新 tag 仍是 `v0.8.4`、没有任何 tag 含本线的提交
（版本号三处仍都是 0.8.4）。发版权在用户手里，要发新版本得用户明确说。

## 5. 怎么跑

```bash
# 桌面应用（本目录 = 块级渲染线）；Vite 固定 1420，别和普通目录同时开
cd /home/z301/Typst-pad-block-geometry && npm run tauri dev

cd /home/z301/Typst-pad-block-geometry
npm run check        # 类型检查
npm test             # 710 项（38 个文件）

# 浏览器验收（headless Chromium；换端口别跟 tauri dev 抢 1420）
npm run dev -- --port 1425
BROWSER_CHECK_PORT=1425 CDP_PORT=9335 node scripts/browser-check/writing-blocks.mjs
npm run fixtures:blocks
BROWSER_CHECK_PORT=1425 CDP_PORT=9335 node scripts/browser-check/writing-blocks-visual.mjs
BROWSER_CHECK_PORT=1425 CDP_PORT=9335 node scripts/browser-check/writing-blocks-hit.mjs
BROWSER_CHECK_PORT=1425 CDP_PORT=9335 node scripts/browser-check/writing-mode-scenes.mjs
BROWSER_CHECK_PORT=1425 node scripts/browser-check/wysiwyg.mjs
```

验收几个容易踩的点（都是实测踩过的）：

- **同一个 CDP 页面不许两套验收并发跑** —— 会互相改 `localStorage`，
  报 `SecurityError: Failed to read the 'localStorage' property`，结果作废。串行跑。
- 块夹具必须在 **600px 视口**下量（`Emulation.setDeviceMetricsOverride`）：
  1400px 视口会把夹具拉伸 2.125×，几何对比全部失效（真机比例是 1.333）。
- 真实文档夹具：`cargo test dump_real_doc_fixture -- --ignored --nocapture`
  （用户那份数学作业文档在 `.browser-check/real-scene.typ` / `real-fixture.json`）。
- 真实引擎的"这个块有没有输出"要直接看块表（`found && svg === ""`），
  **不能**看 `buildBlockCovers` 的结果 —— 那条路会把"这一轮没渲"标成 revealed，于是永远不补渲。

## 6. 和这位用户协作的老规矩（本仓库通用）

- 改 `src/` 会 HMR 到正在运行的 `tauri dev`；改 `src-tauri/` 会让 Tauri 重启应用。
- **绝不阻塞等 GitHub workflow**（不写轮询、不用 `job_output(wait: true)` 等 CI/Release）；
  报告状态最多**单次**查一次 `gh run list`。
- **发版权在用户手里**：改版本号三处 / 写 CHANGELOG / 打 tag / 发 Release，
  这四件事只在用户明确说「发 X.Y.Z」之后才做。
- **只清理自己创建的对象**：自己起的进程记 PID，自己建的文件自己删，
  别碰用户开着的窗口、端口、编辑器会话。
- 观感偏好：仿 Typora，**不要工具条**；菜单/Alt 不许夺走编辑区焦点；界面不要多余色块与凸出。
- 他不喜欢等慢测试：日常 `npm run check` + 相关单测即可，浏览器那套只在改编辑器/装饰/布局时跑。
