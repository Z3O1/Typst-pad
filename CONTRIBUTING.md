# 贡献指南

先按[开发设置](docs/development/setup.md)准备环境并启动应用；领域边界见[架构](docs/development/architecture.md)。文档与代码注释以中文为主。

## 开发流程

1. 获取最新 `origin/main`，在独立分支上工作，保留已有未提交内容。
2. 对 bug 先确认最小复现和预期行为；按[文档索引](docs/README.md)只读相关领域说明。
3. 做范围清晰的改动，涉及行为变化时同步对应权威文档；不要在 README 或 agent 指南复制实现规范。
4. 按[测试矩阵](docs/development/testing.md)完成检查，审阅完整 diff，清除自己的临时产物。

## 最低验证要求

纯文档改动检查相对链接、旧路径引用和 diff。代码改动至少完成相关类型/静态检查和单元测试，提交前完成测试文档列出的对应 CI 门禁。

编辑器输入、装饰、布局、组件样式、公式或块渲染需要额外 browser verification。浏览器桩不能证明真实文件写盘、原生对话框、多窗口隔离或更新安装正确；改到这些链路需要桌面验证，并明确记录未覆盖的平台。

测试要证明用户行为与失败路径；不要只断言实现细节或靠放宽几何阈值消除失败。审查时同时读代码契约和运行证据，重点检查成功/失败 IPC、过期结果、数据丢失、权限及回退路径。

## Commit 与 PR

- 提交标题写明具体变化；提交应聚焦一个目的，不混入格式化全仓、生成物、密钥或无关重构。
- PR 描述说明问题、最终行为、验证方法与结果、未验证边界；修复问题时给出最小触发步骤。
- UI 变化附相关截图或浏览器测量证据；性能变化说明输入与测量环境，避免把一次计数写成长期保证。
- 审查发现附文件位置、触发条件、影响和修复建议；区分亲自验证、推断与未覆盖事项。审查不能替代测试，测试通过也不能替代契约审查。
- 发布授权与不可逆操作遵循 [AGENTS.md](AGENTS.md)；版本事实来自配置，发行历史只在 [CHANGELOG](CHANGELOG.md) 维护。

## 报告 bug 或 regression

通过 [Issues](https://github.com/Z3O1/Typst-pad/issues) 提供：应用版本（帮助 → 关于）、系统与运行方式、编辑模式、最小 `.typ` 示例、重现步骤、预期与实际结果。涉及字体/包时附族名和导入语句；通过状态栏复制诊断，敏感路径或正文先脱敏。

回归请注明最后正常版本或提交，以及是否只在桌面 WebView 出现。光标、滚动和输入法问题附操作顺序、缩放档位和截图/录屏；无需上传个人完整文档或任何签名密钥。
